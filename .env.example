# Site URL (used for sitemap, Open Graph, etc.)
NEXT_PUBLIC_SITE_URL=https://seconddraftdigital.com

# Contact form: when using static export, set this to your Azure Function or form endpoint URL.
# The form will POST JSON to this URL. Leave unset to use the built-in /api/contact (dev only; not available with static export).
# NEXT_PUBLIC_CONTACT_API_URL=https://your-api.azurewebsites.net/api/contact

# Optional: enable sending email from API route (only when not using static export)
# CONTACT_EMAIL_ENABLED=true
